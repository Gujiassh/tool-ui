import { DocsHeader } from "../_components/docs-header";
import { ActionsExamples } from "./actions-examples";

<DocsHeader
  title="Actions"
  description="Display surfaces stay display-only. Render LocalActions and DecisionActions as siblings."
  mdxPath="app/docs/actions/content.mdx"
/>

## Model

- `LocalActions`: runtime side effects only, no `addResult(...)`.
- `DecisionActions`: consequential actions that commit a typed decision envelope.
- Display components: render data only, plus intrinsic controls.

<ActionsExamples />
